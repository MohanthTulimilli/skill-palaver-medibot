# Medibots Health – Docker Compose (EC2)
# Copy to .env and set values: cp .env.example .env

# Database (MariaDB)
DB_PASSWORD=changeme_strong_password
DB_ROOT_PASSWORD=changeme_root_strong_password
DB_USER=medibot

# Backend
JWT_SECRET=changeme_random_string_at_least_32_characters_long

# CORS – set to your EC2 public URL (no trailing slash)
# e.g. http://YOUR_EC2_PUBLIC_IP or https://your-domain.com
CORS_ALLOWED_ORIGINS=http://YOUR_EC2_PUBLIC_IP
